user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen       80;
        server_name  localhost;

        # Root route (/) - Serve from dist-ssg
        location = / {
            root   /usr/share/nginx/html/dist-ssg;
            try_files /dist-ssg/index.html =404;
        }

        # Static assets for SSG
        location /assets/ {
            root   /usr/share/nginx/html/dist-ssg;
            try_files $uri =404;
        }

        # All other routes - Serve from dist-csr
        location / {
            root   /usr/share/nginx/html;
            try_files /dist-csr$uri /dist-csr$uri/ /dist-csr/index.html =404;
        }

        # Error pages
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
} 